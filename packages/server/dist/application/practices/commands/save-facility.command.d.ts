export declare class SaveFacilityCommand {
    readonly code: number;
    readonly name: string;
    readonly county: string;
    readonly mechanism: string;
    readonly _id?: string;
    constructor(code: number, name: string, county: string, mechanism: string, _id?: string);
}
